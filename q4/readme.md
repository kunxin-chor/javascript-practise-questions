Create a function named `leftOrRight` which takes in two integer arguments. Return the string `left`
if the first integer is larger, returns `right` if the second integer is larger. If both are the same,
return a `same`.